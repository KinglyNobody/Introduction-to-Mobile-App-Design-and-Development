import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'labworkGmail',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
