import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wolf-app-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
